POSTGRES_USER=athena
POSTGRES_PASSWORD=athena_dev
POSTGRES_DB=athena
SMTP_USE_SSL=true
SMTP_HOST=smtp.exmail.qq.com
SMTP_FROM_EMAIL=webmaster@wxbooks.cn
SMTP_USER=webmaster@wxbooks.cn
SMTP_PASSWORD=3abGLXGxWtWvgJmz
SMTP_PORT=465
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123

# ============ Athena 服务端口配置（4XXXX 段）============
# 开发环境使用，避免与 WxLibrary 等其他服务冲突

# Web 入口和 API
TRAEFIK_PORT=48080
API_PORT=48000
WEB_DEV_PORT=48173

# 存储和搜索
SEAWEED_S3_PORT=48333
SEAWEED_ADMIN_PORT=48888
OPENSEARCH_PORT=49200

# 其他服务
TOLGEE_PORT=48085
CALIBRE_UI_PORT=48081
CALIBRE_WEB_PORT=48082

# ============ PowerSync Service 配置 (Phase 0) ============
# @see 09 - APP-FIRST架构改造计划.md

# PowerSync Service 端口
POWERSYNC_PORT=48090
POWERSYNC_METRICS_PORT=49091

# PowerSync 是否允许上传 (staging 环境可设为 false 进行只读演练)
POWERSYNC_UPLOAD_ENABLED=true

# PowerSync JWT Secret (需与前端公钥配对)
# 建议使用 openssl rand -base64 32 生成
POWERSYNC_JWT_SECRET=dev_powersync_secret_change_in_production

# 注意：POWERSYNC_DATABASE_URL 在 docker-compose.yml 中使用 ${POSTGRES_PASSWORD} 动态构建
# 无需在此重复定义
AUTH_SECRET=dev_powersync_secret_change_in_production

# ============ 认证会话配置 (App-First 模式) ============
# Access Token 有效期：24 小时 (86400 秒)
ACCESS_EXPIRE=86400
# Refresh Token 有效期：30 天 (2592000 秒) - App-First 模式保持长期登录
REFRESH_EXPIRE=2592000
